{
  "method": "POST",
  "path": "/api/v1/auth/password/reset/request",
  "authentication": "none (public endpoint)",
  "contentType": "application/json",
  "headers": {},
  "notes": "Always returns 200 regardless of whether the email exists (prevents email enumeration). The raw token is printed to server stdout in DEMO MODE.",
  "scenarios": {
    "success_known_email": {
      "description": "Email matches an existing account. Token logged to backend stdout in DEMO MODE.",
      "body": {
        "email": "registered.user@example.com"
      },
      "expectedStatus": 200
    },
    "success_unknown_email": {
      "description": "Email is not in the database. Response is identical to the success case (enumeration protection).",
      "body": {
        "email": "nobody@example.com"
      },
      "expectedStatus": 200
    },
    "validation_invalid_email_format_422": {
      "body": {
        "email": "not-an-email"
      },
      "expectedStatus": 422
    },
    "validation_missing_email_422": {
      "body": {},
      "expectedStatus": 422
    }
  }
}
